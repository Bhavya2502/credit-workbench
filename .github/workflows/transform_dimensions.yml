name: transform-dimensions
# Every dimensioned fact — the note schedules — plus the axis index and tag catalog.
on:
  workflow_dispatch:
    inputs:
      parts:
        description: "all | build | flag | register | verify"
        required: true
        default: all
        type: choice
        options: [all, build, flag, register, verify]

env:
  MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
  R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
  R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}

jobs:
  build:
    if: inputs.parts == 'all' || inputs.parts == 'build'
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      max-parallel: 3
      fail-fast: false
      matrix:
        # Finer batches for recent years: filings carry far more dimensioned detail
        # now than in 2009, so an even split by year would not be an even split by row.
        archives: ["2009-2012", "2013-2015", "2016-2018", "2019-2020",
                   "2021-2022", "2023-2023", "2024-2024", "2025-2026"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync
      - run: uv run python -m credit_workbench.transform.dimensions --archives ${{ matrix.archives }}

  flag:
    # Ranks the filings that report each figure, so a query gets one vintage rather
    # than the four-or-so copies the lake holds. Batched by period year because every
    # copy of a figure shares its period end.
    if: always() && (needs.build.result == 'success'
                     || inputs.parts == 'flag' || inputs.parts == 'all')
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      max-parallel: 3
      fail-fast: false
      matrix:
        period_years: ["2008-2013", "2014-2016", "2017-2018", "2019-2020",
                       "2021-2022", "2023-2023", "2024-2024", "2025-2030", "tail"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync
      - run: uv run python -m credit_workbench.transform.dimensions --period-years ${{ matrix.period_years }}

  register:
    if: always() && (needs.flag.result == 'success' || inputs.parts == 'register')
    needs: [build, flag]
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync
      - run: uv run python -m credit_workbench.transform.dimensions --register
      - name: Verify
        run: uv run python -m credit_workbench.transform.verify_dimensions

  verify:
    if: inputs.parts == 'verify'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync
      - run: uv run python -m credit_workbench.transform.verify_dimensions
