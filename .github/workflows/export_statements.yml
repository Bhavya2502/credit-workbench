name: export-statements
# Financial statements for every company - line items down, fiscal years across -
# published as a run artifact.
on:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync
      - run: uv run python -m credit_workbench.warehouse.export_statements
      # Two artifacts so the workbook can be downloaded without the long CSV.
      - uses: actions/upload-artifact@v4
        with:
          name: credit-workbench-statements
          path: export/credit_workbench_statements.xlsx
          retention-days: 14
      - uses: actions/upload-artifact@v4
        with:
          name: credit-workbench-statements-long-csv
          path: export/statements_long.csv.gz
          retention-days: 14
